class Arbre:
    def __init__(self, ref, binval, val , current_binary , is_code , children):
        self.ref = ref
        self.binVal = binval
        self.val = val
        self.current_binary = current_binary
        self.is_code = is_code
        self.children = children


